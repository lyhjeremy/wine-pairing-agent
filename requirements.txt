# LangGraph agent + local RAG retrieval tool. All local, no API key required.
langgraph>=1.0
pandas>=2.0
sentence-transformers>=3.0
chromadb>=0.5
# Optional: only needed if you run the API backend instead of the Claude CLI.
anthropic>=0.40
